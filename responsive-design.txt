정답은 없음.

어떤 시도도 이유만 괜찮다면, 다 정답


1. 반응형(화면이 줄어들면, 바로 대응이 됨)
2. 모바일 버전(m.naver.com 모바일 html, css가 따로 있음, 화면 조정해도 실시간 반영 안 됨)

1. 반응형

1-1. CSS(HTML만 쓸때도, React 안 쓰고, CSS만으로 반응형 대응이 힘든 경우가 있음)
1-2. JS까지 써서(JS 써서 하는 것), JS가 없으면 React가 안 돌이감

1-2. 그냥 React는 React답게, javascript 적극적으로 써도 됨.(현재로서는 정답)

JS를 써도 되는데, react-media-query(또는 다른 여러가지 라이브러리) 등 라이브러리를 쓰는 것은 좋아도 
자기가 직접 JS 반응형을 짜는 것은 연습으로서는 좋지만, 실무에서는 쓰지 말 것.
